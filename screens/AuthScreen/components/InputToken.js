import { styled } from 'dripsy';
import { TextInput } from 'react-native';

export const InputToken = styled(TextInput)({
  height: 40,
  margin: 1,
  padding: 2,
  bg: 'inputBG',
  borderRadius: 5,
});
