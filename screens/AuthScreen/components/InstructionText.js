import { styled } from 'dripsy';
import { Text } from 'react-native';

export const InstructionText = styled(Text)({
  color: '#fff',
  textAlign: 'center',
  fontSize: 16,
  margin: 1,
  paddingX: 20,
});
